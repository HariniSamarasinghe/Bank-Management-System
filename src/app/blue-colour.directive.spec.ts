import { BlueColourDirective } from './blue-colour.directive';

describe('BlueColourDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueColourDirective();
    expect(directive).toBeTruthy();
  });
});
